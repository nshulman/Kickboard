<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>cardCss__c</fullName>
    <externalId>false</externalId>
    <formula>IF( ISPICKVAL(Type__c, &apos;Emotions&apos;),&apos;slds-card emotions-color&apos;,
 IF(ISPICKVAL(Type__c, &apos;Facts and Observations&apos;),&apos;slds-card fact-and-observations-color&apos;,
  IF( ISPICKVAL(Type__c, &apos;Goals&apos;),&apos;slds-card goals-color&apos;,
   IF( ISPICKVAL(Type__c, &apos;Metrics&apos;),&apos;slds-card metrics-color&apos;,
    IF( ISPICKVAL(Type__c, &apos;Pain Points&apos;),&apos;slds-card pain-points-color&apos;,
     IF( ISPICKVAL(Type__c, &apos;Systems and Key Information&apos;),&apos;slds-card systems-and-key-information-color&apos;,
      IF( ISPICKVAL(Type__c, &apos;Touchpoints&apos;),&apos;slds-card touchpoints-color&apos;,
       IF( ISPICKVAL(Type__c, &apos;Opportunities and Ideas&apos;),&apos;slds-card opportunity-and-ideas-color&apos;,&apos;&apos;
)
)
)
)
)
)
)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>cardCss</label>
    <required>false</required>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
